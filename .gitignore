*.log
*.zip
logs/
# Per-tenant workspaces are local data, never program content (arch design §3)
workspaces/
dist/
reports/
archive/
.DS_Store
Thumbs.db
*.swp
.vscode/
.codex/memory/
__pycache__/
plans/
docs/plans/
docs/specs/
docs/superpowers/
*.xlsx
config/*.bak
config/tenant-sits.json
config/last-classifier-upload.json
# Per-tenant config directories are local-only (like tenant-fingerprints.json)
config/tenants/
research/
backups/
changepacks/
xml/
data/tp_patterns.json
data/.smbdelete*
scripts/Get-TrainableClassifiers.ps1
scripts/Test-CrossTenantConnect.ps1
scripts/build-australian-names.py
scriptfixes.md
SITcompaction.md
# Pester NUnit/JUnit result artifacts (CI/local test runs) — never tracked
testResults.xml
TestResults.xml

# Test fixtures are program content (committed), even when their paths (xml/, reports/,
# backups/, plans/, logs/, config/tenant-sits.json, config/tenants/) match the broad
# local-data ignores above. The Convert-ToWorkspace migration tests (Task 5A-2) depend on
# tests/fixtures/migration/ being tracked. Re-include the whole fixtures tree.
!tests/fixtures/**
