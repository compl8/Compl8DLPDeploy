# Repo handoff — 2026-06-14 (Stage 4 Engine + DLP-rule drift complete)

> Restart checkpoint. A fresh session can resume cold from this file + git. Trust git over memory.

## Where things stand
- **Branch:** `cleanup-safety-hardening`. **HEAD:** `da0a4cc`. **NOTHING PUSHED** — all work is local (origin = github.com/compl8/Compl8DLPDeploy, master at `5efaef6`).
- **Stages 1–4 of the layered-architecture restructure are DONE** (Compl8.Model → Tenant → Content → Engine), plus a **DLP-rule drift** feature on top.
- **Tests:** full suite **776 passed / 1 skipped / 0 failed** (`scripts/Invoke-CIChecks.ps1`, ~5 min). **Fast suite** (excludes 8 Slow shadow tests that run real leaf scripts) **768 passed / 0 failed**, ~35 s: `scripts/Invoke-CIChecks.ps1 -Fast` or `Invoke-Pester` with `Filter.ExcludeTag='Slow'`.
- **Working tree:** clean except the long-standing untracked-by-design `Test-Compl8Fingerprint.ps1` and `docs/` (gitignored specs/plans).
- **No in-flight background work** — every task gated green, every stage codex-reviewed clean.

## What was built this session (key commits)
- **Stage 4 — Compl8.Engine (assess→plan→apply)**: 25 commits `40e1b85..a86fbf7`. Phases 4A assess / 4B plan / 4C apply (framework + checkpoint/resume + 5 shadow-validated executors) / 4D integration. Plan: `docs/superpowers/plans/2026-06-13-arch-restructure-stage4-engine.md` (all ticked). Every phase boundary codex-reviewed; findings fixed in-stage.
- **DLP-rule drift** (assess now catches hand-edited DLP rules/policies): 6 commits `a86fbf7..c32ff6f`.
  - `Get-DlpRuleContentHash` (Model) — canonical rule-content hash, desired-build and actual-readback shapes compare equal.
  - `Resolve-DesiredDlpRules` (Content) — reads config/{policies,labels,classifiers,settings,rule-overrides}.json; **shadow-proven** to match `Deploy-DLPRules.ps1 -WhatIf` (5 policies + 60 rules, exact names + hashes). Content builders `New-DLPSITCondition`/`New-AdvancedRuleJson` facade-ported to Model.
  - `Get-TenantInventory` (Tenant) carries actual rule/policy content + `contentHash`.
  - `Invoke-Compl8Assess` (Engine) buckets dlpRule/dlpPolicy create/drift/orphan/foreign via a new `-ConfigRoot` seam (the config bridge; re-points to `desired/resolved` in Stage 5).
  - Ownership for rules/policies is **provenance-stamp-driven and PREFIX-SCOPED** (a stamp confers ownership only if it resolves AND its prefix == the inventory prefix; different-prefix/unresolved → foreign — never claims another deployment's objects). Rule/policy names do NOT start with the prefix (templates: dlpPolicy `P{n}-{code}-{prefix}-{suffix}`, dlpRule `P{n}-R{n}-{code}-{label}-{suffix}`), so the old prefix-start `ours` check is wrong for them.
- **Harden:** tagged the 5 slow shadow Describes `Slow` + added `Invoke-CIChecks -Fast` (`da0a4cc`).

## NEXT (first actions for the next session)
1. **Discuss Stage 5** (the user's stated next step) — surface collapse: rewire the TUI/orchestrators onto Engine verbs; leaf `Deploy-*.ps1` → deprecation shims → deleted; one-off `Convert-ToWorkspace`. No Stage 5 plan written yet. Design spec §6 Stage 5.
2. **Before any live use:** run the operator **nonprod shadow-trial checklist** in `docs/superpowers/specs/2026-06-13-engine-lifecycle.md` (record `actual/` from nonprod via `scripts/record-engine-fixtures.ps1`, assess→plan, shadow-diff each executor vs the live old path's `-WhatIf`, then first real dictionary apply). Requires tenant access — operator-only.
3. **Push decision:** 32 commits unpushed on `cleanup-safety-hardening`. Push / PR / merge as desired.

## Open follow-ups (tracked; none blocking)
- **Provenance helper duplication** — `Resolve-DeploymentProvenanceRegistryPath` + `ConvertFrom-DeploymentProvenanceFieldValue` exist in both `DLP-Deploy.psm1` and `Compl8.Model/Private` (intentional migration-period dup, D1). DEFERRED to Stage 5 (clean consolidation needs facade-scope rework once DLP-Deploy dissolves).
- **AutoLabel shadow test is 243 s** (runs `Deploy-AutoLabeling.ps1` live; dominates the full suite). Likely a real per-item sleep in its `-WhatIf` path not neutralized like the DLPRules shadow did with `interCallDelaySec`. Could be sped up; tagged `Slow` for now.
- **Get-TenantInventory sit-level + rule-level records** need validation against a real nonprod recording before the first live assess (CI fixtures are hand-authored to the same shapes).
- Assess drift now covers SIT/package/dictionary AND dlpRule/dlpPolicy. (The Stage-4 gap "assess doesn't detect dlpRule drift" is now CLOSED.)

## Environment gotchas (this Windows machine / Z: share)
- Z: is a network share — git needs `safe.directory` (already configured globally for this repo).
- Use **Pester 5.7.1 user-scope**, NOT inbox 3.4.0 (v3 → mass bogus "BeforeAll/Should operator" failures). Prepend `Import-Module Pester -MinimumVersion 5.0` if in doubt.
- **codex review per stage**: `codex exec review --base <branch-marker>` — `--base` REJECTS a custom prompt arg, run it bare. Markers are throwaway git branches.
- **Shadow-test pattern** (to validate Engine code vs an old leaf SCRIPT): run the REAL script with connection/session/fingerprint as PLAIN GLOBAL stubs (NOT `Mock -ModuleName` — module mocks drive ShouldProcess→$false so mutating cmdlets never fire) + SCC cmdlets as global RECORDING stubs; diff via `Get-Compl8ShadowDiff`.
- Determinism in Engine/Model: `Get-Date`/`Get-Random` are banned in pure paths — inject clocks/ids/timestamps.

## Gate commands
- Full CI: `pwsh -NoProfile -File scripts/Invoke-CIChecks.ps1`
- Fast CI (skip slow shadow): `pwsh -NoProfile -File scripts/Invoke-CIChecks.ps1 -Fast`
- Pester only: `pwsh -NoProfile -Command "Invoke-Pester -Path tests"` (full) — for fast, a `[PesterConfiguration]` with `Filter.ExcludeTag='Slow'`.
