# Repo handoff — 2026-06-15 (Stage 5 Phase 5B complete: Engine-backed surface)

> Restart checkpoint. A fresh session can resume cold from this file + git. Trust git over memory.
> ⚠️ A CONCURRENT AGENT is active in this same Z: working tree — see "Concurrent agent" below.

## Where things stand
- **Branch:** `cleanup-safety-hardening`. **HEAD:** `259beba` (the 5B review-fix). **NOTHING PUSHED** — all local; now **57 commits** ahead of origin (origin master `5efaef6`).
- **Stages 1–4 + DLP-rule drift + Stage 5A + Stage 5B DONE.** The Engine (assess→plan→apply) is fully wired into BOTH surfaces behind an opt-in, all routes default LEAF so **live behaviour is unchanged**.
- **Tests (clean 5B tree, HEAD `259beba`):** fast suite **878 passed / 1 skipped / 0 failed** (8 Slow NotRun) via `scripts/Invoke-CIChecks.ps1 -Fast`. Full suite (~886/1/0) not re-run this session to avoid testing the concurrent agent's in-flight work.
- **Working tree is NOT clean right now:** it carries the concurrent agent's UNCOMMITTED auto-label work (3 modified + 3 untracked) plus the long-standing by-design untracked `Test-Compl8Fingerprint.ps1` and `docs/`. None of that is 5B.

## Stage 5 status (plan: docs/superpowers/plans/2026-06-14-arch-restructure-stage5-surface.md)
- **Phase 5A (offline foundation) DONE 2026-06-14** (`7e0c4bc..346b51b`, codex-clean): New-Compl8Context + tenant.json; Convert-ToWorkspace; config→desired re-point; provenance→workspace; helper dedup.
- **Phase 5B (Engine-backed surface) DONE 2026-06-15**, 4 commits, codex-reviewed:
  - `8cb23f5` **5B-1 `Invoke-Compl8Deploy`** (Compl8.Engine): holistic context→assess→plan→render→confirm→apply. Honours `Context.EngineRoutes` by FILTERING the assessment buckets to the routed object types and planning ONLY those (everything else reported `deferred`, never mutated) + a route-wrapped executor map backstop (un-routed objectTypes deferred; `tenant` snapshot always allowed). `-WhatIf` = plan-only; no routed type ⇒ assess+plan only (no apply). Returns a `compl8.deploy-result/v1` { phase planned|applied; routedTypes; deferredTypes; assessment; plan; render; deferred[]; apply }. 18 tests (`tests/Compl8.Engine.Deploy.Tests.ps1`).
  - `85266c7` **5B-2 CLI** (`scripts/Invoke-FullDeployment.ps1`): opt-in `-UseEngine` (+ `-WorkspaceRoot`) maps the 7-field bundle into New-Compl8Context; each phase (Labels/Dictionaries/Classifiers/DLPRules) routes through Invoke-Compl8Deploy via a SCOPED context WHEN its type's route is ON, else the existing leaf `Deploy-*.ps1` UNCHANGED. `Test-Compl8PhaseRoutesToEngine` is the gate. AST/string wiring + behavioural-gate tests (`tests/Invoke-FullDeployment.Engine.Tests.ps1`).
  - `f034c79` **5B-3 TUI** (`Start-DLPDeploy.ps1`): same opt-in on the per-type deploy menus + the rollout wizard's Labels/Classifiers/DLPRules phases; `Get-Compl8DeployContext` builds the context on demand (defensive: missing pin ⇒ leaf fallback). Guided removal, snapshot/propagation checkpoints, COMPL8_ORCHESTRATED all kept. Tests `tests/Start-DLPDeploy.Engine.Tests.ps1`.
  - `259beba` **REVIEW 5B fix** (codex P1, both surfaces): a routed REAL apply reached the executors with empty DesiredContent → `$null` content → broken create/update. FIX: `Invoke-Compl8EnginePhase` forces PLAN-ONLY (preview) when no DesiredContent is supplied, regardless of -WhatIf. A 5C slice enables real apply by passing `-DesiredContent`. +4 behavioural tests.
  - **Mechanism:** the route→objectType map is dictionary→[dictionary]; label→[label,labelPolicy]; rulePackage→[rulePackage,sit]; dlpRule→[dlpRule,dlpPolicy]; autoLabel→[autoLabelPolicy]. Helpers are PER-SURFACE INLINE (operator chose per-phase routing over a shared module helper) — a possible future consolidation, NOT flagged by codex.

## NEXT — Phase 5C (per-type cutover, OPERATOR-GATED)
For each type in order **dictionaries → labels → rule packages → DLP rules → auto-label**, one vertical slice:
1. **(offline) Wire the type's DesiredContent** into the surface engine-phase call (the resolved terms/XML/condition the executors need) — this is what flips the engine phase from plan-only to real-apply for that type. Until wired, a routed real apply is plan-only (safe by the 5B P1 fix).
2. **⚠️ OPERATOR nonprod shadow trial** (`docs/superpowers/specs/2026-06-13-engine-lifecycle.md`): record `actual/`, assess→plan, confirm an EMPTY `Get-Compl8ShadowDiff` vs the live leaf `-WhatIf`. Route is NOT flipped until clean.
3. **(offline, gated on clean diff)** flip `tenant.json` engineRoutes.<type> = true (the cutover commit); demote that leaf to a deprecation shim.
Then **5D** delete shims; workspace is the only deploy source. Agent CANNOT do step 2 (needs tenant).

## ⚠️ Concurrent agent (READ THIS)
- Another agent is building **auto-label assess coverage** in this SAME working tree: `modules/Compl8.Content/Public/Resolve-DesiredAutoLabel.ps1` (new), autoLabelPolicy buckets in `Invoke-Compl8Assess.ps1`, `Get-TenantInventory.ps1` autolabel extensions, `Compl8.Content.psd1`, + `tests/Compl8.Content.DesiredAutoLabel.Tests.ps1` + `tests/Compl8.Engine.AssessAutoLabel.Tests.ps1` (~696 lines, UNCOMMITTED). It is NOT part of 5B (the user confirmed it's the concurrent agent's work; I restored it after briefly stashing it to verify the clean 5B count).
- **Sharing one working tree:** keep git ops TARGETED — NO `git add .` / `git add -A` / `git commit -a` / `git stash` / `git reset`. `git add <specific files>` only. Otherwise you clobber the other agent's uncommitted work.
- **codex `exec review` runs in WRITE mode here** (codex-cli 0.139.0): during REVIEW 5B it re-touched the autolabel files. Run review bare (`codex exec review --base <throwaway-marker>`) only on a clean tree, or use a read-only sandbox; expect it may edit.

## Environment gotchas (Windows / Z: share)
- Z: is a network share — git needs `safe.directory` (configured). Git background maintenance (`multi-pack-index`/geometric-repack/`renaming pack`) fails with "Permission denied" on the share — these are NON-FATAL noise AFTER a successful commit (the commit lands; verify with `git log -1`).
- Use **Pester 5.7.1 user-scope**; a fresh `pwsh` needs `Import-Module Pester -MinimumVersion 5.0 -Force` before `[PesterConfiguration]` is available (inbox 3.x shadows it).
- `TestPatternDrift.Tests.ps1` prints `RESULT: FAIL` lines for its NEGATIVE fixtures — those are EXPECTED (the test passes; the gate is correctly rejecting bad input).
- Orchestrator/TUI tests are AST/string-based (the `& <path>.ps1` leaf calls aren't mockable) — do NOT execute the orchestrators in tests.

## Gate commands
- Fast CI: `pwsh -NoProfile -File scripts/Invoke-CIChecks.ps1 -Fast`
- Full CI: `pwsh -NoProfile -File scripts/Invoke-CIChecks.ps1`
- Single test file: a `[PesterConfiguration]` with `Run.Path` set (Import Pester 5 first).
