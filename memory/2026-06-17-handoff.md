# Handoff — 2026-06-17 (restart checkpoint)

## Where things stand
- **Branch:** `master`. **HEAD:** `0b3fc43c`. In sync with `origin/master` (pushed).
- **Working tree:** clean of code changes. Untracked-by-design: `Test-Compl8Fingerprint.ps1`, `docs/` (gitignored specs/plans).
- **Full CI:** **1054 passed / 1 skipped / 0 failed** (8 Slow NotRun under `-Fast`). Last run green at HEAD.
- **In-flight background work:** NONE. Every CI + codex task this session completed; nothing dies on restart, nothing to relaunch.

## What shipped this session (all MERGED to master, codex-clean, CI green)
Continued from the reconciliation epic R1–R5 (already merged: `ac80c6b0`). Then, after the operator asked "how good is the planning really?", a **PLANNER-DEPTH** arc and a **STRATEGIST**:

1. **Propagation by real SIT visibility** (`2a8a2338`) — `Test-Compl8Gate` honours `Context.DependencyVisible` (visible→proceed / not-visible→block, authoritative over the clock); `Get-Compl8PlanOrder` stamps `gate.requiresSitIds`; `Invoke-Compl8Apply` takes injectable `-PropagationProbe`; `Get-Compl8SitVisibilityProbe` (Get-DlpSensitiveInformationType-based) is the default in `Invoke-Compl8Deploy`. The 4h offset is now only a FALLBACK.
2. **Policy-drift split** (`70f015f0`) — assess attributes dlpPolicy/autoLabelPolicy drift per-aspect (`driftFields` = mode/label/locations); `Get-Compl8PlanOrder` steps the update only when `driftFields` excludes `locations` (mode/label converges like the leaf; a location change needs recreate → surfaced unreconciled). Also documents the externalRefs gate as a dormant forward hook.
3. **Reference-existence BLOCKER** (`6f677a05`) — a deploy HALTS pre-apply (`phase='blocked-references'`) if a DLP rule it deploys references a tenant identity that doesn't exist. `Get-Compl8ReferenceCandidates` (recipient refs from GenerateIncidentReport/NotifyUser; tag kind=recipient|domain; skip boolean switches) + `Get-Compl8ReferenceReadiness` (exempt SiteAdmin/Owner/LastModifier tokens + kind=domain + external emails; block missing internal) + `Get-Compl8IdentityResolver` (Get-Recipient/Get-AcceptedDomain; Exchange wildcard accepted-domain semantics; fail-safe→unverified never false-missing). Wired as `Invoke-Compl8Deploy` pre-flight `5b`, scoped to routed dlpRule steps, using assess's desired-rule source (dlp-rules.json|config).
4. **STRATEGIST = `Get-Compl8ChangeRisk`** (`4a9e2ea5`) — evaluate+describe risk + follow-on + effects on INTERNAL (ours) vs EXTERNAL (not-ours) rules/classifiers; hand back if too complex. Pure; reuses R3 for the downstream walk; ownership map splits affected objects internal/external (unknown→foreign, conservative); risk critical/high/medium/low; reversible=$false only when destructively damaging not-ours; recommendation proceed/review/hand-back. HAND-BACK (operator policy) = ANY external reach OR cascade>threshold → BLOCK auto-apply pending approval. create/claim + rule/policy changes are additive/self-contained→proceed; only classifier (sit/rulePackage/dictionary) damaging changes analyze downstream. A DICTIONARY remove/update ALWAYS hands back (full fed-sit consumer set unverifiable from recorded state). Wired as `Invoke-Compl8Deploy` pre-flight `5c` (HALTS `phase='blocked-risk'` on unapproved hand-back; `-ApprovedRiskActions`/`-ApproveAllRiskHandBacks`/`-SkipRiskCheck`; risk graph built `-IncludeActualSits`; sit steps keyed by entity GUID).
5. **Strategist SURFACED in the reconcile walk** (`d11a08d5` + `0b3fc43c`) — extracted 2 shared helpers `Get-Compl8OwnershipMap` + `Get-Compl8ReferenceGraph` (`-IncludeActualSits`, `-ConfigRoot`; deploy delegates to it). `Get-Compl8ReconcileCandidates` attaches `.risk` per candidate; `Invoke-Compl8Reconcile` annotates each iteration `.risk` + top-level `riskHandBacks`; `Get-Compl8ReconciliationReport` renders them; the `[M]` menu builds the graph via `Get-Compl8ReferenceGraph -IncludeActualSits` and prints each candidate's risk while prompting. NOTE: the `[M]` graph was previously built from the inventory's recorded packages which carry NO XML → it had NO sit edges at all; `-IncludeActualSits` fixes that. Risk in the walk is INFORMATIONAL (preview never auto-applies); the deploy `5c` gate enforces the block.

Discipline used throughout: per-phase branch → TDD → full `-Fast` CI green → `codex exec review --base <marker>` (often many rounds; every finding was a real correctness/safety issue) → fast-forward merge → push → delete branch + marker.

## NEXT — R6 (the only remaining work; OPERATOR-gated, connected)
The headless agent CANNOT do the interactive WAM login. The operator runs it in their own pwsh.
First action verbatim: **operator connects to compl8.dev (= `nonprod`, prefix QGISCF), records the tenant** (`scripts/record-engine-fixtures.ps1 -Connect -Tenant compl8.dev -TargetEnvironment nonprod -Prefix QGISCF`, READ-ONLY → `workspaces/nonprod/actual/inventory.json`, gitignored), then runs `Start-DLPDeploy.ps1 -UseEngine`, selects target env `nonprod`, picks `[M] Reconcile / Migrate`, and walks the conflicts: the 48 OLD unstamped rules squat the desired names → choose **claim** (each candidate now shows its risk verdict while choosing). Apply claim-only iteration 1, re-record, then reconcile the resulting drift via the per-type deploy path (which runs the 5b reference + 5c risk gates). Open decision: the `TestPattern -` dictionaries can't be claimed (non-claimable type → only `leave` offered; names differ from desired QGISCF placeholders) — so for dicts: create-new + cascade-remove-old, or leave.

Deeper planner gaps still open (NOT in scope of what was asked; flag if pushing further): no cost/impact-weighted step ordering; the graph is shallow WITHIN families (no intra-policy rule ordering, no label hierarchy).

## Standing gate commands
- Full CI (fast): `pwsh -NoProfile -File ./scripts/Invoke-CIChecks.ps1 -Fast` (excludes `Slow` shadow tests; ~100s; green = "All CI checks passed" + "Failed: 0").
- Targeted: `pwsh -NoProfile -Command "Invoke-Pester -Path ./tests/<file>.Tests.ps1 -Output Detailed"`.
- Codex per phase: tag a base marker at the pre-branch master commit, then `codex exec review --base <marker>` (read-only, diff-based; run BARE, no custom prompt arg). Verdict body follows a line `codex`; `[P1]`/`[P2]` = must-fix.

## Environment gotchas (durable)
- Z: is a network share. `git` commits SUCCEED but emit a harmless trailing `fatal: renaming pack ... Permission denied / geometric-repack failed` — IGNORE it (the commit landed; verify with `git log --oneline -1`).
- `git` also warns `LF will be replaced by CRLF` on add — harmless (autocrlf); the line-ending golden robustness was solved at test level (`b2e9e8c`).
- Connected ops (Connect-IPPSSession / WAM) need a window handle the headless agent lacks → the OPERATOR runs all connected steps. EXO `Get-Recipient`/`Get-AcceptedDomain` are present (module installed) but throw when not connected → the resolvers fail-safe to 'unverified' (never a false 'missing').
- Keep git ops TARGETED (no `git add .`/`-a`) — a concurrent agent has shared this tree before.
- Pester 5.7.1; `AfterEach`/teardown must live INSIDE a `Describe` (not at file root). Double-quoted regex strings expand `$var` — use single-quoted patterns for `\$x` literals.

Related memory: [[arch-restructure-arc]], [[win-env-gotchas]], [[session-continuity-skills]]. Prior handoffs: `2026-06-15-handoff.md`, `2026-06-14-handoff.md`.
